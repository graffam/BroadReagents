

<table>
  <thead>
    <tr>
      <% @kits.each do |kit| %>
        <th> <%= kit.name %></th>
      <% end %>
        <th>Needed By</th>
        <th>Comments</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <%= form_for [@machine,@order] do |f| %>
        <%= f.fields_for :kit_orders do |ff| %>
            <td>
              <%= ff.number_field :amount %>
              <%= ff.hidden_field :kit_id %>
            </td>
        <% end %>
        <td>
          <%= f.datetime_field(:needed_by) %>
        </td>
        <td>
          <%= f.text_field :comment %>
        </td>
        <%= f.submit "Place Order" %>
      <% end %>
    </tr>
  </tbody>
</table>
